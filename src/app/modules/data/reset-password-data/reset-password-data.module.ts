import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

import { ResetPasswordDataService } from './services/reset-password-data.service';

@NgModule({
  declarations: [],
  imports: [
    CommonModule,
  ],
  providers: [
    ResetPasswordDataService,
  ],
})
export class ResetPasswordDataModule { }
